FROM node:20-alpine AS base
WORKDIR /app
COPY package.json pnpm-lock.yaml* tsconfig*.json vite.config.ts postcss.config.js tailwind.config.js ./
COPY src ./src
COPY index.html ./
RUN corepack enable && pnpm install --frozen-lockfile || pnpm install
CMD ["pnpm", "dev", "--host", "0.0.0.0"]
